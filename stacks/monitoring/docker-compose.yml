networks:
  server-net:
    external: true

services:

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "100.106.157.117:9090:9090"   # Tailscale only
    volumes:
      - /opt/monitoring/prometheus:/prometheus
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--storage.tsdb.retention.size=14GB'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    networks:
      - server-net

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
      - '--collector.rapl'
      - '--web.listen-address=100.106.157.117:9100'   # Tailscale only
    restart: unless-stopped
    network_mode: host   # needs host network to see all interfaces + mounts

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor
    ports:
      - "100.106.157.117:8082:8080"   # Tailscale only
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /opt/docker:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    privileged: true
    restart: unless-stopped
    networks:
      - server-net

  blackbox:
    image: prom/blackbox-exporter:latest
    container_name: blackbox
    ports:
      - "127.0.0.1:9115:9115"   # localhost only — only Prometheus needs this
    volumes:
      - ./blackbox/blackbox.yml:/etc/blackbox_exporter/config.yml:ro
    restart: unless-stopped
    networks:
      - server-net

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3001:3000"
    volumes:
      - /opt/monitoring/grafana:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/etc/grafana/dashboards
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_ROOT_URL: https://${GRAFANA_DOMAIN}
      GF_SERVER_DOMAIN: ${GRAFANA_DOMAIN}
      GF_SECURITY_ALLOW_EMBEDDING: "true"
      DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL}
    restart: unless-stopped
    networks:
      - server-net
